#! /bin/bash

answer=$(echo -e "Yes\nNo" | dmenu -i -p \
    "Are you sure you want to power down?")

if [ "${answer}" = "Yes" ] ; then
    shutdown now
elif [ "${answer}" = "No" ] ; then
    :
else
    shutdown ${answer}
fi
