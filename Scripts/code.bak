#! /bin/bash


function selectDirectory() {
}

if [[ $# -eq 1 ]] ; then
    case $1 in
        -a) echo "Option 'a' was found" ;;
        -b) echo "Option 'b' was found" ;;
    esac

elif [[ $# -eq 2 ]] ; then
    case $2 in
        -c) vim $HOME/Coding/${directory}/OOP/ ;;
        *) echo $(echo $2 | cut -c 2-): invalid flag
            exit 111
            ;;
    esac
fi
